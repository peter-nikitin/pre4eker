import React from 'react'
import PropTypes from 'prop-types'

const Order = ({order}) => {
  return (
    <div>
      
    </div>
  )
}

Order.propTypes = {
  order: PropTypes.object
}

export default Order
