import response from '../../../data/response';

console.log(response);


export const responceReducer = (state = response, { type, payload }) => {
  switch (type) {
 
  default:
    return state
  }
}
